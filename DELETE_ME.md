# Historical source notice

This file originally described this repository as a disposable deployment snapshot and pointed to an unspecified private Origin repository.

That instruction is superseded for the September 4, 2026 release: the owner explicitly requested that the redesigned dashboard and project documentation be pushed to this repository's main branch and deployed to the existing Vercel project. This repository now retains the source and release documentation for that update. This file does not direct repository deletion.

The private Origin copy was not accessible during the release and its current state is unverified. Reconcile it with GitHub main before allowing an Origin-based workflow to overwrite production.

See [deployment documentation](docs/DEPLOYMENT.md).
